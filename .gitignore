# 依存パッケージ(npm installで復元できるため追跡しない)
node_modules/

# ビルド生成物(npm run buildで再生成できるため追跡しない。
# 配布用インストーラーはGitHub Releasesなどでの配布を想定)
dist/
*.exe

# 共有時はブラウザ版のみに限定するため、インストール版一式はリポジトリに含めない
# (ローカルには残す。npm install等が必要な場合は手元で行う)
インストール版/

# OS/エディタ由来
.DS_Store
Thumbs.db
